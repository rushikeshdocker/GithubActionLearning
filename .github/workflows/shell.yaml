name: Test github action
on: push


jobs:
  shell-test:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello World"
      
      - name: multiline script
        run: |
          npm -v
